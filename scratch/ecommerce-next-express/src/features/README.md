# Storefront features

Feature folders are reserved for home, products, categories, product detail, search, cart, wishlist, authentication, profile, orders, checkout, and admin dashboard views. Keep UI state local and use `src/shared/api/client.ts` for server communication.
