# OS Adapter

Cross-platform operations handling path separators and platform binaries.

## API

- `normalizePath()`: Replaces backslashes with slashes.
- `isPathSafe()`: Detects and stops directory traversal.
