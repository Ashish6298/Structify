# Template Variable Engine

Interpolates variables dynamically inside template blueprints.

## Traversal Protections

Blocks strings that request directory traversal (e.g. `../../`) before parsing begins.
