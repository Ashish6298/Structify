{
  "name": "structify-mcp-server",
  "version": "1.0.0",
  "description": "Model Context Protocol server exposing Structify capabilities to AI agents",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node dist/index.js",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@structify/core": "*",
    "@structify/logger": "*"
  },
  "devDependencies": {}
}
